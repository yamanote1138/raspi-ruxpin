# Raspi Ruxpin Environment Configuration - macOS Development
# Copy this file to .env for Mac development
#
# This configuration uses mock GPIO and system audio for development
# without Raspberry Pi hardware.

# Application Settings
ENVIRONMENT=development
DEBUG=true
HOST=0.0.0.0
PORT=8080

# Hardware Configuration - Eyes Servo
# Note: Pin numbers don't matter with mock GPIO, but keep them valid for config validation
HARDWARE__EYES_PWM=21
HARDWARE__EYES_DIR=16
HARDWARE__EYES_CDIR=20
HARDWARE__EYES_SPEED=100
HARDWARE__EYES_DURATION=0.8  # Slower for 40+ year old servos

# Hardware Configuration - Mouth Servo
HARDWARE__MOUTH_PWM=25
HARDWARE__MOUTH_DIR=7
HARDWARE__MOUTH_CDIR=8
HARDWARE__MOUTH_SPEED=100
HARDWARE__MOUTH_DURATION=0.3  # Slower for 40+ year old servos

# Hardware Configuration - GPIO
# IMPORTANT: Set to true for Mac development (auto-detected, but explicit is better)
HARDWARE__USE_MOCK_GPIO=true

# Audio Configuration
# On Mac: Uses system audio via afplay and AppleScript for volume
AUDIO__MIXER=default
AUDIO__START_VOLUME=50  # Mac system volume (0-100)
AUDIO__SAMPLE_RATE=16000
AUDIO__AMPLITUDE_THRESHOLD=500
AUDIO__SOUNDS_DIR=sounds

# Text-to-Speech Configuration
# Install espeak on Mac: brew install espeak
TTS__ENGINE=espeak
TTS__VOICE=en-us+m7
# Voice options: en-us (default), en-us+m2 (male2), en-us+m7 (male7), en-us+f3 (female3)
TTS__SPEED=140
# Speed: 80-200 is typical (higher = faster)
TTS__PITCH=40
# Pitch: 0-99 (50 is default, lower = deeper, higher = higher pitched)
TTS__OUTPUT_DIR=sounds/tts

# Configuration Files
CONFIG_DIR=config
PHRASES_FILE=config/phrases.json
# Optional: Override hardware config with YAML
# HARDWARE_CONFIG_FILE=config/hardware.yaml

# Mac Development Notes:
# - GPIO operations are mocked and logged to console
# - Servos won't physically move (no hardware)
# - Audio plays through Mac speakers via afplay
# - Volume control uses AppleScript (requires macOS)
# - WebSocket and UI work exactly like production
# - Check backend logs to see mock GPIO operations
